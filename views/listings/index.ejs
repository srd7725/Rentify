<!-- <% layout("layouts/boilerplate") %>

<h3>All Listings</h3>

<% for (let listing of allListings){ %>

<div class="card" style="width: 18rem;">
<img src="<%= listing.image.url %>" class="card-img-top" alt="listing_image"/> 
   <div class="card-body">
    <h5 class="card-title"><%= listing.title %></h5>
    <p class="card-text">
      <%= listing.description %>
    </p>
  </div>
</div>

<% } %>

<ul>
  <% for (let listing of allListings) { %>
    <li>
      <a href="/listings/<%= listing._id %>">
        <%= listing.title %>
      </a>
    </li>
  <% } %>
</ul> -->



<% layout("layouts/boilerplate") %>

<h3>All Listings</h3>

<% for (let listing of allListings){ %>

<!-- 🔥 anchor add kiya card ko clickable banane ke liye -->
<a href="/listings/<%= listing._id %>" style="text-decoration: none; color: black;">

<div class="card" style="width: 18rem;">
  <img src="<%= listing.image.url %>" class="card-img-top" alt="listing_image"/> 
  
  <div class="card-body">
    <!-- title already correct tha -->
    <h5 class="card-title"><%= listing.title %></h5>

    <!-- description bhi correct -->
    <p class="card-text">
      <%= listing.description %>
    </p>
  </div>
</div>

</a> <!-- 🔥 yeh close karna mat bhoolna -->

<% } %>
